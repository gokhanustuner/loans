package com.hubs.loans.dto;

public class InstallmentResponse {
    /*
    private Long id;
    private BigDecimal amount;
    private BigDecimal paidAmount;
    private LocalDate dueDate;
    private boolean isPaid;

    public InstallmentResponse(LoanInstallment installment) {
        this.id = installment.getId();
        this.amount = installment.getAmount();
        this.paidAmount = installment.getPaidAmount();
        this.dueDate = installment.getDueDate();
        this.isPaid = installment.isPaid();
    }

     */

    // Getters and setters
}
